const arrayFirst = [5, 10, 15, 20];
const arraySecond = [10, 20, 30];
const concatArray = arrayFirst.concat(arraySecond);
let result = 0;

for (number of concatArray) {
  result += number;
}

console.log(result);
